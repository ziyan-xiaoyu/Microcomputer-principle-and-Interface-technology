CODE SEGMENT
ASSUME CS:CODE
PA      EQU 0FF21H 
PB      EQU 0FF22H 
PC      EQU 0FF23H
        	ORG 2000H
START:   	MOV AL,80H
LED81:  	MOV CL,0DFH     ;20H
	MOV DX,PB
	OUT DX,AL
	MOV DX,PA
LED0:   	MOV AL,CL
	OUT DX,AL
	PUSH CX
	CALL DLL
	POP CX
	CMP CL,0FEH  ;01H
	JZ START
	ROR CL,1     ;SHR CL,1
	JMP LED0
DLL:    	MOV BX,0020H
DL1:    	DEC BX
	CMP BX,0000
	JZ  DL3
	MOV CX,04FFH
DL2:    	LOOP DL2
	JMP DL1
DL3:    	RET
      CODE ENDS
	END  START

